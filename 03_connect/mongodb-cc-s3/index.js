const express = requires("express");
const mongoose = requires("mongose");

const app = express();

// conect to mondodb db.
function connect(params) {
  // mongodb+srv://<username>:<password>@cluster0.hqerf8q.mongodb.net/?retryWrites=true&w=majority
  // abc, abc
}
